<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Spotui" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="splashScreenTheme" parent="Theme.SplashScreen">
        <!-- Same disco-ball artwork as the launcher icon, so the splash flash
             matches the icon the user tapped (not the old equalizer logo). -->
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Spotui</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
</resources>

